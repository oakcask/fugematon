interface:
  display_name: "GitHub PR Operations"
  short_description: "Create, edit, and merge pull requests with GitHub CLI"
  default_prompt: "Use $github-pr-operations when creating, editing, merging, or managing pull requests with GitHub CLI."
