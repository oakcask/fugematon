# Generator Constraint Rebuild

## Status

In progress. The completed slices have added constraint evidence and removed several focused repair surfaces. The latest slice completes solver ownership for important exposition-entry tails: the finally shaped draft is returned to constraint evaluation, and bounded held, staggered, linear, and oblique support candidates prevent unexplained outside-voice collapse before score adoption.

The regenerated standard bundle selects relaxation `none` for all 22 seeds with 0 public hard-contract, density, or structural-label failures. Important-entry and first-bass-answer zero-outside windows both fall from 22 to 0. Mechanical coupling, exact collision, accented entry clash, mechanical reuse, and metrical-boundary cost remain at their pre-slice baselines; leap-recovery misses rise from 1164 to 1169 across four control seeds and remain a localized `score-concern`. Agent-side score review accepts the implementation slice; manual listening remains an optional non-blocking gap. No aesthetic count is promoted to `ci-observed` or `ci-blocking`.

## Preservation Contract

The rewrite preserves the public `GenerationOutput` event envelope, diagnostics compatibility, seed determinism, known voice set, safe tick and MIDI value shape, subject and answer plan identity, key metadata consistency, and the active `WritingProfile` pitch/range continuation contract.

Legacy exact note sequences and beauty-metric expected values are not preservation requirements. They may change when the solver gives the same structural guarantees directly. Review-only metrics such as line agency, lockstep, phrase recurrence, unison duration, local sentinels, and surface brilliance remain adoption evidence unless a later source-backed plan promotes a focused rule.

## Architecture Target

Continuation generation must construct bounded local `SectionConstraintProblem` instances before notes are committed to the score. Each section candidate must include:

* voice/time slots classified as `note`, `hold`, or internal `intentional-rest`;
* allowed rest reasons tied to section function, cadence, entry handoff, register relief, suspension resolution, or pedal thinning;
* required active-voice coverage for non-cadential continuation windows;
* structural root / chord-tone support at harmonic anchors;
* entry handoff and subject-fragment continuity constraints;
* independent contour / rhythm soft costs;
* deterministic candidate ids, hard-failure lists, soft-cost reasons, relaxation level, and bounded search width.

Backtracking is the desired behavior when a candidate creates unexplained silence or unsupported texture collapse. The search should first try alternative voicing, support-line, rest-reason, register, and local-harmony assignments inside the section. If all bounded candidates fail, the selected section must expose the relaxation reason in `constraintSatisfactionReview` and `generatorSearchTrace`; it must not be silently repaired after generation.

## Current Target

Proceed from the accepted important-entry-tail slice to the next solver-owned architecture work without waiting for manual listening. Exposition candidate construction now searches held, staggered, alternate-voice, and local-harmony support before adoption, and final boundary / voice-order shaping is included in the evaluated draft. Density and structural guarantees remain candidate-owned, and score-level support cleanup does not change the normally adopted score.

The implementation slice is present, and the primary seven plus standard 22-seed score evidence has no unresolved high-confidence `score-blocking` finding. Remaining exact-collision and leap-recovery windows stay `score-concern`, and pairwise preference stays `listening-gap`. `constraintSatisfactionReview` and the entry-tail / musical-independence counts remain `review-required`; no new CI hard gate is introduced.

## Completed Foundation

The current completed slice ports the non-continuation exposition boundary and entry-local support handling to deterministic local search. Initial generation builds exposition-local candidates before continuation generation, evaluates each with the constraint-core evaluator, and emits a solver-mode `generatorSearchTrace` with real exposition candidate ids.

Entry-local support instability now affects constraint-core soft costs and continuation candidate entry-harmony scoring. Entry diagnostics distinguish unresolved severe support from carried suspension-like support, weak passing / neighbor support, and stepwise release, while entry-boundary continuity classifies important subject / answer windows as prepared collective articulation or continuity-supported rather than unexplained synchronized reset.

The completed episode / free-counterpoint slice adds local constraint evidence for selected continuation sections while keeping the later repair passes visible. The solver trace now includes section-local candidate ids, and those candidate rows summarize episode motivic derivation, cadence / entry preparation, free-counterpoint harmony realization, independent contour / rhythm, clash resolution, and entry handoff support as soft-cost explanations. Subject-fragment episode entries are included in entry-boundary continuity windows, and exposed free-counterpoint solo / thinning windows are promoted into score-window acceptance.

The terminal-support slice adds terminal continuation-candidate soft costs for cadence target preparation, low-voice root / chord-tone support, outer-voice landing, final-rest stability, and unsupported texture collapse. Terminal-support costs now appear in `generatorSearchTrace` reason strings without overriding the established continuation-quality selection gates, while `terminalClosureReview` keeps continuous-fugue terminal closure as `not-required` without hiding weak low-voice support, unsupported collapse, or final-rest failures.

The first final-repair downgrade ports harmonic-stasis rearticulation cleanup out of the `generateScore` final pass. Section-local continuation candidates now include harmonic-stasis solver variants, and score-level support cleanup records unrepaired / repaired trace rows before adopting the repaired notes. Other final repair surfaces remain open until their matching solver replacement and focused before / after review evidence are present.

The harmonic-continuity support slice ports short pivot-episode support cleanup into score-level deterministic local search. Score-level rows now preserve unrepaired and repaired harmonic-continuity evidence before adoption, reject repairs that increase hard failures, and keep remaining review-required continuity windows visible rather than accepting them without audible score support.

The score-level support cleanup trace slice completes the remaining focused support cleanup downgrade for functional thinning, post-entry continuation support, long-rest phrase closure, bass-answer tail texture support, and texture voice-crossing repair. The score-level adoption loop keeps unrepaired / repaired trace evidence, rejects hard-failure regressions, and keeps the standard 22 seed texture voice-crossing surface closed as generated no-op evidence: normal continuation texture is already built with adjacent voice-order checks, texture-role-aware octave placement, harmonic anchor support, and active `WritingProfile` pitch / range limits before the score-level surface runs. A refreshed target review confirmed the current 22 seed bundle is score-identical to the generated baseline, keeps 0 hard-contract failures and 0 `voiceCrossings`, and still emits 0 generated `texture-voice-crossing-repair` paired rows. A focused synthetic score remains the regression fixture for the real texture crossing before / after delta.

The continuous-fugue segment-continuation trace slice adds continuation-local boundary candidates for segment 1 and later. The solver trace now emits `segment-<index>-boundary-continuation-*` rows for hidden boundaries, keeps hard failures separate from boundary soft costs, and scores carry, pedal support, staggered re-entry, prior-tail harmonic support, first-attack density, role mix, and hard-restart risk without changing the public output envelope, seed determinism, voice set, key metadata, or `WritingProfile` pitch contract.

The constrained-profile feasibility slice makes key and subject planning consult the active `WritingProfile` before entries are accepted. Constrained profiles now choose profile-realizable key / subject / answer combinations, require harmonic-anchor bass-root and chord-tone support in the profile domain, widen exposition entry spacing only when the chosen subject cannot maintain adjacent order in the active profile, run adjacent voice-order search inside exposition / texture candidate construction, penalize exposed high-register soprano leaps during candidate scoring, and replace final pitch projection with a no-op invariant candidate. If profile projection would still change a note, diagnostics expose the remaining hard failure instead of silently mutating the score.

The section-slot CSP diagnostics slice adds an internal `SectionConstraintProblem` model for continuation sections. Voice/time slots are classified as `note`, `hold`, or internal `intentional-rest`; allowed rest reasons are `cadence-breath`, `entry-handoff-delay`, `register-relief`, `suspension-resolution`, and `pedal-thinning`. The public `ScoreEvent` contract remains note/meta only. `constraintSatisfactionReview` schema 7 reports section windows, density, structural-support, and metrical-boundary classifications, min/max active voices, intentional rest spans, unplanned silent runs, infeasible constraint counts, selected relaxation level, deterministic solver candidate count, harmonic-quality evidence, planned-entry / voice-pair pressure, and voice-agency evidence. Only function-unexplained density collapse and unsupported support-role structural labels enter the corresponding focused hard rejections; legitimate passing, neighbor, suspension, function-explained thinning, prepared pickup, planned-entry pickup, and cadence rhetoric remain visible accepted classifications. Harmonic-anchor quality, entry support, severe intervals, unison pressure, lockstep pressure, sustained dissonance, and voice-agency counts remain review-required soft costs.

The continuation-CSP selection slice adds a `section-csp` continuation candidate band and a duration-candidate layer. Common-time continuation prefers 8q / 12q measure-aligned sections, retains odd 6q / 7q / 9q / 10q relaxed candidates as metrical soft alternatives, and evaluates duration candidates from forked PRNG snapshots when metrical placement requires relaxation. Section-CSP variants reuse the existing section-local support cleanup logic for functional thinning, exposed free-counterpoint solo support, post-entry continuation support, long-rest phrase closure, bass-answer tail texture support, harmonic-continuity support, and voice-order repair before score adoption. `chooseContinuationSection` evaluates continuation candidates with `sectionConstraintProblem: true`, prefers candidates without hard failures, and relaxes deterministically by hard-failure count, soft cost, and candidate id when all candidates remain infeasible. Score-level support cleanup no longer mutates the normal score path; it remains audit fallback evidence through unrepaired / proposed rows until the standard bundle shows full no-op or failure-evidence behavior.

The continuation-CSP candidate-domain slice expands section-CSP variants before score adoption. The existing section-local, grammar, and phrase-family candidate pool now tries low-root-first and balanced upper-agency support policies with bass-root structural anchor support, long-rest closure, bass-answer tail support, harmonic-continuity support, and sustained-dissonance repair timing. Backtracking now considers structural harmonic support hard failures as well as voice-coverage failures. The standard bundle improves several target pressure surfaces to 3 seeds at no relaxation, 12 at density-floor review, 3 at structural-support review, 4 infeasible, 34 unsupported-solo windows, 0 all-voice-silence windows, and max unplanned silent run 2880 ticks. Metrical boundary cost remains active at 3146 with 72 unprepared transitions. Remaining non-chord structural-support, density-floor, and metrical evidence keeps the CSP surface review-required.

The density and structural-support slice closes the remaining target hard failures. Section candidates add density-floor support before adoption, normalize structural intent against realized pitches, and repair or reclassify support-role non-chord tails without chord-tone-normalizing legitimate passing, neighbor, or suspension behavior. Candidate evaluation may omit detailed classification rows while searching to bound memory, but selected section diagnostics retain the classifications and aggregate counts. Score-level cleanup still emits audit rows but never adopts their proposed notes on the normal path. The refreshed standard bundle has all 22 seeds at relaxation `none`, with 0 unsupported density windows, min-active violations, unsupported solo windows, long-unplanned-silence violations, unsupported structural labels, or non-chord structural-support hard failures.

The voice-pair independence and planned-entry preparation slice separates functional synchronization from mechanical coupling in both diagnostics and section-local scoring. Unprepared off-measure planned entries can add low-root-first or balanced-upper-agency harmonic pickup support without moving the entry, while mechanical free-counterpoint boundaries can try deterministic half-beat staggering variants. Planned-entry support notes retain note-level motivic provenance, and pair classification accepts synchronization only when entry preparation is explicit or both notes share entry, cadence, or sequence function. Pareto selection rejects local improvements that worsen entry / harmonic support, voice agency, metrical cost, leap recovery, or mechanical reuse. Mechanical coupling duration, exact collision, and color doubling also have individual non-regression guardrails so a weighted improvement cannot hide deterioration on another voice-pair axis. The bounded voice-pair review list reserves representatives from every functional and review-required classification before filling by duration; shorter coupling windows therefore remain available for focused listening even when long subject support dominates the score. Trace rows expose stable stage-specific candidate ids, hard failures, soft-cost reasons, relaxation, and selection reasons; score-level repair remains non-adopting audit evidence.

The entry-window continuity slice generalizes first-bass-answer tail texture into important-entry multi-voice windows. `importantEntryTailTexture` reads subject / answer / episode subject-fragment entries from entry start through entry end plus four beats, counting zero / one already-entered outside voices at half-beat checkpoints. Exposition and continuation candidates can now add `entry-window` support variants before selection, and `important-entry-tail-texture` soft cost ranks unresolved thinning without making deliberate thinning a hard failure. The legacy `bassAnswerTailTexture` remains as a first-bass-answer compatibility and rhythm-coherence surface.

The important-entry-tail ownership slice closes the gap between candidate evaluation and the adopted exposition draft. Boundary shaping and voice-order processing now occur inside each evaluated exposition candidate, zero / one-voice runs split when their active outside-voice signature changes, and the effective scan is no longer truncated at the nominal exposition-plan boundary before the answer ends. The bounded domain evaluates finalized base, oblique continuity, alternate linear voicing, and held-or-staggered support. Selection first removes the unexplained zero-outside cost without increasing hard failures, then preserves mechanical coupling, exact collision, accented entry clash, leap recovery, and mechanical reuse when the domain permits it. The selected candidate and all rejected alternatives flow into the final solver trace. Review evidence is recorded in [Generator Constraint Rebuild Important Entry Tail Review](../reviews/generator-constraint-rebuild-important-entry-tail.md).

## Implementation Order

1. Keep the existing subject, answer, section, harmony, and `WritingProfile` builders as solver input.
2. Evaluate generated windows with hard failures separated from soft costs. Complete for the diagnostics-only slice.
3. Port exposition generation first so subject identity, answer identity, entry order, key metadata, and profile pitch contracts are produced by search rather than repair. Complete for the non-continuation exposition slice: initial generation now selects between two local candidates before continuation generation.
4. Add entry-local support constraints for prepared consonance/dissonance handling, carried outside voices, staggered attacks, and post-entry continuity. Complete for important subject / answer entry windows; remaining subject-fragment episode entry support belongs to the later episode / free-counterpoint slice.
5. Add episode and free-counterpoint constraints for motivic derivation, harmony realization, independent contour/rhythm, and clash resolution. Complete: section-local continuation candidates now enter `generatorSearchTrace` with stable candidate ids and soft-cost explanations for episode / free-counterpoint behavior; score-window acceptance structurally classifies subject-fragment entry support and exposed free-counterpoint solo / thinning.
6. Downgrade or remove repair passes only after the corresponding solver constraint directly guarantees the behavior. Terminal-support candidate costs are now in place for terminal continuation windows. Harmonic-stasis rearticulation, harmonic-continuity support, and the focused score-level support cleanup group are downgraded with before / after trace evidence. Texture voice-crossing repair is closed as score-level solver-owned for any generated score delta, while the standard generated bundle is classified as synthetic-only / no-op because the normal generator path structurally prevents adjacent texture voice-order crossings before that surface has a diff to adopt. Continuous-fugue boundary carry repair now records continuation-local before / after or adopted boundary candidate evidence instead of relying on diagnostics-only trace rows.
7. Make constrained `WritingProfile` pitch projection an invariant rather than the cleanup mechanism. Complete for key / subject / harmonic-anchor feasibility and the reported `music-box-n20` voice-order regressions: final projection now emits a no-op candidate when generation satisfied the profile and a hard-failure candidate when it would have needed to rewrite notes.
8. Model continuation voice/time slots as a local finite-domain CSP. Complete only for the first diagnostic and constraint-evaluator slice: section rows expose note/hold/internal-rest slots, allowed rest reasons, min active voice coverage, unsupported solo, all-voice silence, long unplanned silence, root/chord structural support, bounded candidate count, and relaxation level. The normal public event surface still has no rest event.
9. Make continuation section construction CSP-owned. Complete for density, structural support, and metrical-boundary planning: continuation candidates classify function-explained thinning, legitimate non-chord behavior, and prepared off-measure rhetoric; add required density support; normalize unsupported structural labels; and search measure-landing duration alternatives for unprepared boundaries before adoption.
10. Add deterministic backtracking across section-local alternatives. Complete for the target pressure surfaces: selection ranks stable candidate ids by hard failures, soft cost, and relaxation, and all 22 standard seeds now select relaxation `none`. Further candidate-domain work remains possible for review-only musical tradeoffs.
11. Retire matching score-level repair surfaces from the normal path. Complete for the focused support-cleanup group: the rows remain diagnostics-visible no-op or failure evidence, and no score-level cleanup proposal is selected as the normal adopted score.
12. Promote calibrated CSP outcomes only after review evidence. Complete for the current slice: schema 7 classifications and functional voice-pair evidence separate target false positives and true failures, agent-side score review has no unresolved blocker, and manual listening remains a non-blocking gap. The metrics stay `review-required`; no new CSP or musical-independence count becomes `ci-blocking` in this slice.

## Theory Inputs

Use source-family evidence before promoting musical preferences into hard gates:

* Fux/species counterpoint for consonance, dissonance preparation/resolution, motion, and line independence.
* `common-practice-fugue-subjects` for subject and answer identity, recognizability, and answer-compatible rhetoric.
* `common-practice-fugue-episodes` for episode derivation, sequencing, imitation, and subject-free spans that still serve form.
* `keyboard-playability-and-compass` for `WritingProfile` pitch and playability separation.
* Generative music evaluation sources for candidate scoring and adoption evidence.
* `music-constraint-programming` for local-first finite-domain section constraints, declarative hard/soft rule separation, and the policy of deferring external solver dependencies until local bounded search fails measured targets.

Exact citations or new claim-map entries are required when a later slice changes gate scope, review acceptance, phase scope, or source-backed solver behavior.

## Verification

Current-contract hard failures are tested as unit-level rejects for range, known voice, safe event shape, pitch/velocity bounds, voice crossing, subject identity, answer plan, key metadata, and `WritingProfile` pitch violations.

Integration verification keeps `generateScore` deterministic, schema-compatible, and diagnostics-visible. Non-continuation initial generation should report solver-mode exposition candidates. Continuous-fugue segment continuation now reports solver-mode segment-boundary candidates with stable ids and boundary soft-cost reasons, while weak carry and hard-restart classifications remain visible through `continuousBoundaryCarry`.

The entry-local support slice is reviewed in [Generator Constraint Rebuild Entry Support Review](../reviews/generator-constraint-rebuild-entry-support.md). The 22 seed target bundle keeps review-policy hard failures at 0, lowers aggregate `entrySupportInstabilityCount` and `unresolvedEntrySupportInstabilityCount`, and lowers `unresolvedSevereEntryIntervalCount` plus `unresolved-entry-severe-interval` local sentinels. Remaining max-4 local counters are documented as prepared / continuity-classified important entries or subject-fragment episode work for the next solver slice.

The episode / free-counterpoint slice is reviewed in [Generator Constraint Rebuild Episode Free-Counterpoint Review](../reviews/generator-constraint-rebuild-episode-free-counterpoint.md). The 22 seed target bundle keeps review-policy and hard-contract failures at 0, emits real section-local candidate ids for every seed, and classifies subject-fragment entry support, exposed free-counterpoint solo / thinning, harmonic stasis rearticulation, harmonic continuity, and entry severe-interval windows into accepted context, review-required, or generator response.

The terminal-support slice is reviewed in [Generator Constraint Rebuild Terminal Support Review](../reviews/generator-constraint-rebuild-terminal-support.md). The 22 seed target bundle keeps hard-contract failures at 0, emits terminal-support reason coverage in every terminal candidate row, and keeps continuous-fugue terminal support weaknesses classified as review-visible `not-required` closure evidence rather than final-repair success.

The first final-repair downgrade is reviewed in [Generator Constraint Rebuild Final Repair Downgrade Review](../reviews/generator-constraint-rebuild-final-repair-downgrade.md). The refreshed 22 seed bundle keeps the target hard failures, reference-profile status, entry/support counters, terminal closure classifications, and harmonic-stasis generator-response windows stable. `generatorSearchTrace` exposes `score-harmonic-stasis-unrepaired-final-repair-evidence` beside `score-harmonic-stasis-solver-repaired` for the 10 seeds where score-level support cleanup needs the solver replacement.

The harmonic-continuity support slice is reviewed in [Generator Constraint Rebuild Harmonic Continuity Support Review](../reviews/generator-constraint-rebuild-harmonic-continuity-support.md). The refreshed 22 seed check keeps hard failures, unsupported solo runs, and abrupt texture drops at 0. `generatorSearchTrace` exposes `score-harmonic-continuity-unrepaired-final-repair-evidence` beside `score-harmonic-continuity-solver-repaired` for the 13 seeds where score-level support cleanup improves the harmonic-continuity support surface. Remaining review-required harmonic-continuity windows stay visible and are not treated as solved without score support.

The score-level support cleanup trace slice is reviewed in [Generator Constraint Rebuild Support Cleanup Trace Review](../reviews/generator-constraint-rebuild-support-cleanup-trace.md). The remaining support cleanup group now snapshots the unrepaired score, builds one focused repaired candidate, rejects hard-failure regressions, and emits paired `score-*-unrepaired-final-repair-evidence` / `score-*-solver-repaired` rows before adopting the repaired score. The slice exposes functional thinning, post-entry continuation, long-rest phrase closure, and bass-answer tail support as generated solver-owned adoption evidence. Texture voice-crossing repair stays solver-owned for real score deltas, but the standard 22 seed generated bundle is explicitly closed as synthetic-only / no-op because normal generation preserves adjacent voice order through texture-role priority, harmonic-anchor support, and active `WritingProfile` pitch / range constraints before score-level cleanup.

The continuous-fugue segment-continuation trace slice is reviewed in [Generator Constraint Rebuild Continuation-Local Solver Review](../reviews/generator-constraint-rebuild-continuation-local-solver.md). The normal 22 seed bundle keeps ScoreEvent JSON, review-policy metrics, and exposition / section / score trace coverage identical to the target baseline. A focused segment 1 `continuous-fugue` bundle now emits stable `segment-1-boundary-continuation-*` candidate rows for all 22 seeds, separates hard failures from boundary soft costs, and keeps weak carry or hard-restart evidence visible through `continuousBoundaryCarry`.

The constrained-profile feasibility slice is reviewed in [Music-box n20 voice-crossing structural review](../reviews/music-box-n20-voice-crossing-structural-review.md). Focused regression coverage for `seed-04fup6t-1rmrxhp` now keeps `music-box-n20`, `music-box-n40`, and `four-voice-default` at 0 voice crossings, range violations, writing-profile pitch violations, subject-identity violations, answer-plan violations, and key metadata mismatches. The same test asserts final `WritingProfile` projection is a no-op for each profile and that the 3/4 q=9-12 `music-box-n20` soprano window does not choose A6. Follow-up coverage for `seed-1wudr38-0fbqzth` keeps `music-box-n20`, `four-voice-default`, and `harpsichord-manual` at 0 hard profile / entry / key failures; the music-box profile now has `nonChordStructuralSupportCount = 0` and reduced harmonic-sonority generator-response evidence while thin sonority evidence remains review-required.

The section-slot CSP slice is covered by focused unit tests for allowed intentional-rest reasons, unsupported solo / long unplanned silent runs, structural root/chord support, seed determinism, and the public no-`RestEvent` contract. Music-theory review for the implementation and the 22 seed standard diagnostics bundle is recorded in [Generator Constraint Rebuild Section CSP Review](../reviews/generator-constraint-rebuild-section-csp.md). The standard bundle confirms the CSP surface is useful planner-pressure evidence but not adoption-ready: all 22 seeds currently reach infeasible relaxation, with repeated unplanned silence, unsupported-solo tick windows, and structural support misses. `constraintSatisfactionReview` therefore remains `review-required` until the continuation planner or CSP exception model is repaired and revalidated; no CSP counter should become a CI hard gate from this slice alone.

The continuation-CSP candidate-domain slice is reviewed in [Generator Constraint Rebuild Continuation CSP Review](../reviews/generator-constraint-rebuild-continuation-csp.md). Focused tests cover measure-aligned duration candidate ordering, prepared-pickup versus unprepared offbeat metrical cost, feasible-CSP preference, deterministic all-infeasible relaxation ordering, structural-support backtracking, generated section-CSP trace rows, and the public no-rest-event contract. The refreshed 22 seed standard bundle at 129600 ticks keeps the target relaxation evidence visible while improving several pressure surfaces: 3 seeds select `none`, 12 select `density-floor-review`, 3 select `structural-support-review`, 4 remain `infeasible`, unsupported-solo windows fall to 34, all-voice-silence windows are 0, and max unplanned silent run is 2880 ticks. Metrical boundary cost remains active at 3146, with 72 unprepared transitions against 709 prepared pickups. Score-level support cleanup rows remain fallback evidence and no score-level cleanup row is selected as the normal adopted score path. The slice is still not adoption-ready because density-floor relaxation, non-chord structural-support evidence, and metrical-boundary evidence remain across the standard bundle.

The follow-up density and structural-support slice is reviewed in [Generator Constraint Rebuild Density and Structural Support Review](../reviews/generator-constraint-rebuild-density-structural.md). Focused tests distinguish function-classified entry-handoff thinning, legitimate passing support, and unsupported structural labels. The regenerated 22 seed standard set at 129600 ticks selects relaxation `none` throughout and records 0 unsupported density windows, min-active violations, unsupported solo windows, long-unplanned-silence violations, unsupported structural labels, and non-chord structural-support hard failures while preserving all hard contracts. Score-level cleanup emits 194 audit rows but selects none. Metrical-boundary cost and documented seed-local voice-independence / phrase-development tradeoffs remain review-required, and manual listening remains open.

The metrical-boundary slice is reviewed in [Generator Constraint Rebuild Metrical-Boundary Review](../reviews/generator-constraint-rebuild-metrical-boundary.md). Schema 7 classifies both edges of each continuation section by state, planned-entry identity, cadence preparation, and pickup support. Deterministic duration search adds nearby measure-landing candidates when the current start is displaced, while prepared entry and cadence rhetoric carry zero boundary cost. The regenerated 22 seed set reduces unprepared transitions from 71 to 20 and metrical cost from 3340 to 480, keeps relaxation `none` for every seed, and preserves 0 density / structural and public hard-contract failures. Voice-pair and phrase tradeoffs remain review-required, and manual listening remains open.

The voice-pair independence slice is reviewed in [Generator Constraint Rebuild Voice-Pair Independence Review](../reviews/generator-constraint-rebuild-voice-pair-independence.md). The regenerated 22 seed set keeps relaxation `none` and 0 hard-contract, density, and structural-label failures while reducing function-aware mechanical lockstep pressure from 18423 to 364, unison overlap from 11763 to 11365, unresolved accented entry-clash windows from 31 to 23, leap-recovery misses from 2247 to 1164, and mechanical-reuse windows from 197 to 89. These are score-window and diagnostics results, not a completed listening decision; functional imitation and cadence rhetoric remain manual-listening evidence.
